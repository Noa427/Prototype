# Phases D–H Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Implémenter les phases D–H du projet AEVUM (notifications, campagnes, widget, CRM, calendrier).

**Architecture:** FastAPI backend + React/Vite frontend, modèles SQLModel déjà en place. Chaque phase s'appuie sur les routers existants sans réécrire les modèles.

**Tech Stack:** FastAPI, SQLModel, React, Lucide-react, Tailwind CSS

---

## BUG 1 — POST /api/leads/notify (URGENT)

**Files:**
- Modify: `backend/api/leads.py` (ajouter route)

- [ ] Ajouter `POST /api/leads/notify` dans `backend/api/leads.py`
- [ ] Tester: `curl -X POST http://localhost:8000/api/leads/notify`
- [ ] Commit: `fix: ajouter POST /api/leads/notify manquant`

## BUG 2 — chat-widget.js (URGENT)

**Files:**
- Create: `backend/static/chat-widget.js`

- [ ] Créer dossier `backend/static/`
- [ ] Créer `backend/static/chat-widget.js` (JS vanilla, bouton flottant)
- [ ] Monter StaticFiles dans `backend/main.py`
- [ ] Commit: `fix: créer chat-widget.js et servir static`

---

## Phase D — Notifications

**Files:**
- Modify: `backend/api/leads.py` (déjà corrigé via BUG 1)
- Create: `backend/api/notifications.py`
- Modify: `backend/main.py` (ajouter router)
- Modify: `src/components/Header.jsx` (cloche + badge + dropdown)

- [ ] Créer `backend/api/notifications.py` (GET /, PUT /{id}/read, DELETE /clear)
- [ ] Enregistrer router dans `backend/main.py`
- [ ] Modifier `Header.jsx` : polling 30s, badge, dropdown
- [ ] Commit: `phase-D: notifications temps réel`

---

## Phase E — Campagnes email/SMS

**Files:**
- Create: `backend/api/campaigns.py`
- Create: `backend/services/email_service.py`
- Modify: `backend/main.py`
- Create: `src/pages/Campaigns.jsx`
- Modify: `src/components/Sidebar.jsx`
- Modify: `src/App.jsx`

- [ ] Créer `backend/api/campaigns.py` (CRUD + send)
- [ ] Créer `backend/services/email_service.py` (SMTP ou simulation)
- [ ] Enregistrer router
- [ ] Créer `src/pages/Campaigns.jsx`
- [ ] Ajouter route `/campaigns` dans App.jsx et Sidebar.jsx
- [ ] Commit: `phase-E: campagnes email sms`

---

## Phase F — Chat Widget Embeddable

**Files:**
- Create: `backend/static/chat-widget.js` (déjà amorcé BUG 2)
- Create: `backend/api/chat_public.py`
- Modify: `backend/main.py`

- [ ] Étendre `chat-widget.js` (qualification complète, booking)
- [ ] Créer `backend/api/chat_public.py` (POST /widget, POST /book-slot, GET /widget-config/{key})
- [ ] Enregistrer router
- [ ] Commit: `phase-F: chat widget embeddable`

---

## Phase G — Pipeline CRM & Reporting

**Files:**
- Modify: `backend/api/leads.py` (ajouter /stats, /export)
- Create: `backend/services/report_service.py`
- Modify: `src/pages/Leads.jsx` (vue Kanban)
- Create: `src/pages/Reporting.jsx`
- Modify: `src/components/Sidebar.jsx`
- Modify: `src/App.jsx`

- [ ] Ajouter `GET /api/leads/stats` et `GET /api/leads/export` (CSV)
- [ ] Créer `backend/services/report_service.py` (PDF rapport)
- [ ] Ajouter vue Kanban dans `Leads.jsx` (dnd-kit)
- [ ] Créer `src/pages/Reporting.jsx` (Recharts)
- [ ] Ajouter /reporting dans Sidebar + App.jsx
- [ ] Commit: `phase-G: pipeline crm et reporting`

---

## Phase H — Sync Calendrier Externe

**Files:**
- Create: `backend/services/calendar_service.py`
- Modify: `backend/api/calendar_config.py`
- Create: `backend/api/booking.py`
- Modify: `backend/main.py`
- Modify: `src/pages/CalendarSettings.jsx`

- [ ] Créer `backend/services/calendar_service.py` (parse iCal, cache 15min)
- [ ] Ajouter `GET /api/calendar/slots` et `POST /api/calendar/book`
- [ ] Créer `backend/api/booking.py` (GET /booking/{license_key} page publique)
- [ ] Migration Alembic pour BookedSlot
- [ ] Étendre CalendarSettings.jsx (status sync)
- [ ] Commit: `phase-H: sync calendrier et booking public`
